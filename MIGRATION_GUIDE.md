# Migration Guide: Role Permissions System

## คำแนะนำสำหรับรัน Migration

### 1. รัน Migration บน VPS

เชื่อมต่อ VPS และรัน migration script:

```bash
# SSH เข้า VPS
ssh root@157.245.156.65

# ไปที่ directory ของโปรเจค
cd /apps/Line_price

# Pull code ล่าสุด
git pull origin main

# รัน migration script
psql -U postgres -d line_price_ai -f sql/migrations/002_create_role_permissions.sql
```

### 2. ตรวจสอบว่า Migration สำเร็จ

```bash
# เข้า psql
psql -U postgres -d line_price_ai

# ตรวจสอบ table
\dt role_permissions

# ตรวจสอบข้อมูล
SELECT role, COUNT(*) as permission_count 
FROM role_permissions 
GROUP BY role 
ORDER BY role;

# ควรได้ผลลัพธ์:
#   role     | permission_count
# -----------+------------------
#  admin     |               10
#  operator  |                8
#  worker    |                2
# (3 rows)

# ออกจาก psql
\q
```

### 3. Restart Application

```bash
# Rebuild application
npm run build

# Restart PM2
pm2 restart line-price-ai

# ตรวจสอบ logs
pm2 logs line-price-ai --lines 50
```

### 4. ทดสอบระบบ

1. เข้าหน้า **จัดการสิทธิ์การใช้งาน** (`/admin/roles`)
2. ตรวจสอบว่าโหลดข้อมูลสิทธิ์จาก database ได้
3. ลองเปลี่ยนสิทธิ์ของ `operator` หรือ `worker`
4. กดปุ่ม **บันทึกการตั้งค่า**
5. Refresh หน้าเพื่อดูว่าข้อมูลถูกบันทึกจริง

### 5. Rollback (ถ้าเกิดปัญหา)

```bash
# ลบ table role_permissions
psql -U postgres -d line_price_ai -c "DROP TABLE IF EXISTS role_permissions CASCADE;"

# ระบบจะกลับไปใช้ hardcoded permissions ใน src/lib/permissions.ts
```

---

## การทำงานของระบบใหม่

### **Architecture**

```
┌─────────────────────────────────────────────────┐
│  Frontend: RoleManager.tsx                      │
│  - แสดง UI สำหรับจัดการสิทธิ์                   │
│  - เรียก API เพื่อโหลด/บันทึกข้อมูล              │
└─────────────────────────────────────────────────┘
                     ↓ ↑
┌─────────────────────────────────────────────────┐
│  API: /api/admin/roles                          │
│  - GET: ดึงข้อมูลสิทธิ์ทั้งหมด                  │
│  - PUT: บันทึกสิทธิ์ของ role                    │
└─────────────────────────────────────────────────┘
                     ↓ ↑
┌─────────────────────────────────────────────────┐
│  Database: role_permissions table               │
│  - เก็บสิทธิ์แบบ dynamic                        │
│  - มี cache 1 นาที เพื่อลด query                │
└─────────────────────────────────────────────────┘
```

### **Files Changed**

1. **`sql/migrations/002_create_role_permissions.sql`** - Migration script
2. **`src/app/api/admin/roles/route.ts`** - API endpoints (GET, PUT)
3. **`src/app/admin/roles/RoleManager.tsx`** - UI component
4. **`src/lib/permissions.ts`** - Permission helper functions

### **Features**

- ✅ บันทึกสิทธิ์ลง database แบบ real-time
- ✅ Cache permissions 1 นาที เพื่อ performance
- ✅ Fallback ไปใช้ hardcoded permissions ถ้า database error
- ✅ ป้องกันการแก้ไขสิทธิ์ของ admin
- ✅ Log การเปลี่ยนแปลงสิทธิ์

---

## Troubleshooting

### ปัญหา: Migration ไม่สำเร็จ

```bash
# ตรวจสอบ error
psql -U postgres -d line_price_ai -f sql/migrations/002_create_role_permissions.sql 2>&1 | tee migration.log

# ดู error log
cat migration.log
```

### ปัญหา: API ไม่ทำงาน

```bash
# ตรวจสอบ PM2 logs
pm2 logs line-price-ai --err

# ตรวจสอบ database connection
psql -U postgres -d line_price_ai -c "SELECT 1;"
```

### ปัญหา: สิทธิ์ไม่อัปเดต

```bash
# Clear cache โดยการ restart
pm2 restart line-price-ai

# หรือรอ 1 นาที (cache จะหมดอายุ)
```

---

## Notes

- ⚠️ **อย่าลบ FALLBACK_PERMISSIONS** ใน `src/lib/permissions.ts` - ใช้เป็น backup
- ⚠️ **Admin permissions** ไม่สามารถแก้ไขได้ (hardcoded)
- 💡 Cache TTL = 60 วินาที (ปรับได้ที่ `src/lib/permissions.ts:50`)
