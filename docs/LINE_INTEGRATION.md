# LINE Messaging API Integration

## 📋 ภาพรวม

ระบบส่งข้อความแจ้งเตือนไปยัง LINE Group เมื่อมีการอัปโหลดรูปภาพราคาใหม่

**หมายเหตุ:** ส่งเฉพาะข้อความ (ไม่ส่งรูปภาพ)

---

## 🔧 การตั้งค่า

### 1. สร้าง LINE Messaging API Channel

1. เข้า [LINE Developers Console](https://developers.line.biz/console/)
2. สร้าง Provider (ถ้ายังไม่มี)
3. สร้าง Messaging API Channel
4. คัดลอก **Channel Access Token**

### 2. ตั้งค่า Environment Variable

เพิ่มใน `.env.local`:
```bash
LINE_CHANNEL_ACCESS_TOKEN=your_channel_access_token_here
```

### 3. เพิ่ม Bot เข้า LINE Group

1. สแกน QR Code ของ Bot จาก LINE Developers Console
2. เพิ่ม Bot เข้ากลุ่ม LINE ที่ต้องการ
3. คัดลอก **Group ID** จากการส่งข้อความทดสอบ

---

## 🗄️ Database Migration

รัน migration เพื่อเพิ่มฟิลด์ `line_group_id`:

```bash
# เข้า PostgreSQL
psql -U postgres -d line_price_db

# รัน migration
\i sql/migrations/002_add_line_group_id.sql

# ตรวจสอบ
\d price_groups
```

---

## 📝 การใช้งาน

### 1. ตั้งค่า LINE Group ID

1. เข้าหน้า **จัดการกลุ่มราคา**
2. เลือกกลุ่มที่ต้องการ
3. กรอก **LINE Group ID** 
4. บันทึก

### 2. อัปโหลดรูปภาพ

เมื่ออัปโหลดรูปแรก ระบบจะส่งข้อความไป LINE Group อัตโนมัติ:

```
📢 อัพเดทราคาวันที่ 04/02/2569 21:30

📸 [ชื่อกลุ่มราคา]
```

---

## 🔍 วิธีหา LINE Group ID

### วิธีที่ 1: ใช้ LINE Bot API
```bash
curl -X GET \
  'https://api.line.me/v2/bot/profile/{userId}' \
  -H 'Authorization: Bearer {channel_access_token}'
```

### วิธีที่ 2: ใช้ Webhook
1. ตั้งค่า Webhook URL ใน LINE Developers Console
2. ส่งข้อความในกลุ่ม
3. ดู `source.groupId` จาก webhook event

---

## 📂 ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | คำอธิบาย |
|------|----------|
| `src/lib/line.ts` | ฟังก์ชันส่งข้อความไป LINE API |
| `src/app/api/admin/upload/route.ts` | API อัปโหลดรูป + ส่งข้อความ |
| `src/app/admin/manage-groups/GroupForm.tsx` | ฟอร์มตั้งค่า LINE Group ID |
| `src/types/index.ts` | TypeScript types |
| `sql/migrations/002_add_line_group_id.sql` | Database migration |

---

## 🎯 ฟีเจอร์

- ✅ ส่งข้อความแจ้งเตือนเมื่ออัปโหลดรูปแรก
- ✅ รองรับหลาย LINE Group (แยกตามกลุ่มราคา)
- ✅ ไม่ส่งรูปภาพ (ส่งเฉพาะข้อความ)
- ✅ รองรับทั้ง LINE และ Telegram พร้อมกัน
- ✅ Error handling และ logging

---

## 🔐 Security

- Channel Access Token เก็บใน environment variable
- ไม่ hardcode token ในโค้ด
- ตรวจสอบ permission ก่อนส่งข้อความ

---

## 🐛 Troubleshooting

### ข้อความไม่ส่ง
1. ตรวจสอบ `LINE_CHANNEL_ACCESS_TOKEN` ใน `.env.local`
2. ตรวจสอบ LINE Group ID ถูกต้อง
3. ตรวจสอบ Bot อยู่ในกลุ่ม LINE
4. ดู error log ใน console

### Bot ไม่สามารถส่งข้อความ
1. ตรวจสอบ Channel Access Token ยังใช้งานได้
2. ตรวจสอบ Bot ไม่ถูกบล็อกในกลุ่ม
3. ตรวจสอบ Messaging API ยังเปิดใช้งานอยู่

---

## 📊 API Reference

### `sendLineMessage(groupId, message)`
ส่งข้อความไปยัง LINE Group

**Parameters:**
- `groupId` (string) - LINE Group ID
- `message` (string) - ข้อความที่ต้องการส่ง

**Returns:**
- `{ success: boolean, error?: string }`

### `createPriceUpdateMessage(groupName)`
สร้างข้อความแจ้งเตือนอัพเดทราคา

**Parameters:**
- `groupName` (string) - ชื่อกลุ่มราคา

**Returns:**
- `string` - ข้อความที่จัดรูปแบบแล้ว
